# Progress

## 2026-05-23

- User identified ZIP upload failure as a symptom of broader PRD-to-implementation drift.
- Confirmed current available tools do not expose the `mcp__codex__codex` tool named in AGENTS instructions; using available read-only sub-agent supervision instead.
- Created planning workspace `.planning/prd-implementation-audit`.
- Launched read-only supervision auditors:
  - `Sagan`: material center and ZIP import.
  - `Dewey`: route, menu, permissions, API boundaries.
  - `Hypatia`: task/listener/rules/account-security/business workflows.
- Extracted PRD main headings and confirmed `docs/tg-ops-platform-prd.md` contains product, interface, technical, and test acceptance requirements, so audit must check page, API, permission, audit, and manual behavior.
- Added confirmed findings F002 and F003 for missing material import job/result flow and partial material-center page operations.
- Merged material-center supervision result from `Sagan`; it independently confirmed ZIP import, material import jobs, groups, refresh-cache, and references are incomplete or missing.
- Ran static API surface comparison for PRD-documented endpoints versus backend routers and frontend calls.
- Added findings F004 to F007 for material API drift, source-filter override audit semantics, message sending naming/detail/precheck drift, and listener/operation-metrics partial APIs.
- Merged business-closure supervision result from `Hypatia`; added findings F008 to F013 for account batch entry, listener loop, target admission failure handling, message-send failure rollup, rules version governance, and operation issue handling UX.
- Merged route/permission supervision result from `Dewey`; added findings F014 to F016 for frontend button gating, permission naming drift, and sensitive-read permission verification.
- User clarified material runtime config input: system settings should accept cache channel link / `@username` / `t.me/c/...` and parse to runtime peer. Added F017 as a separate P1 finding rather than treating it as only part of generic material API drift.
- Implementation pass started for the first user-visible P1 slice: F001/F002 ZIP material import and F017 material/source-media cache channel config. Added persistent import jobs, cache config persistence, API routes, worker resolution, frontend system-settings inputs, ZIP upload routing, and regression tests. Pending independent "紫禁城" review before considering this slice accepted.
- Independent "紫禁城" review passed on fourth review for first slice F001/F002/F017 after fixing real image header validation, avatar-pack import mode, persistent import result UI detail, cache-channel friendly errors, and conservative Telethon permission probing.
- Implementation pass completed for F005 source-filter override endpoint before independent review: added `POST /api/tasks/{task_id}/source-filter-overrides`, task-local source identity append, audit detail with sender/action/reason/actor, frontend task-detail button routing to the dedicated endpoint, UI manage-permission gating, and regression tests proving published rule versions are not mutated.
- Independent "紫禁城" first review blocked F005 because `source_action` was not enforced by the backend schema. Fixed by requiring `source_action_id` or `source_action` and adding an endpoint test that missing source action returns 422.
- Independent "紫禁城" review passed F005 after the source action enforcement fix.
- Implementation pass started for F008 account batch action-first flow. The account list batch action buttons now open the batch drawer without requiring preselected rows; the drawer owns batch account selection with search, status filters, select-current-filter, cross-page preserved selection, and preselected-row quick carry-in. Pending independent review.
- Independent "紫禁城" first review blocked F008 because account selection could change after precheck while the old precheck remained visible. Fixed by tracking a payload signature and clearing precheck, batch result, confirmation text, and edited preview overrides whenever account/action/reason/profile/avatar payload inputs change.
- Independent "紫禁城" review passed F008 after the payload-signature stale-precheck fix.
- Implementation pass completed for F009 listener center loop before independent review: added listener events/errors/reset-watermark APIs, source identity fields in listener events, source-state/object error listing, audited watermark reset with reason and confirmation text, listener center UI actions for viewing events/errors and resetting watermarks, and frontend `listeners.manage` gating for switch/reset controls.
- Independent "紫禁城" review passed F009.
- Implementation pass completed for F010 operation target admission retry before independent review: target detail now exposes account-level admission status, failure reason, retryability, and failed-count stats; added `POST /api/operation-targets/{target_id}/admission/retry` to re-read selected failed accounts through the Telegram group snapshot path, refresh group/target capabilities, and audit the operator reason plus retry/recovered/failed counts; Operation Targets UI now shows failed admission reasons and single/all failed retry controls.
- Verification before review for F010: `pytest backend/tests/test_operations_center_runtime.py -k 'operation_target'` passed 9 selected tests; `pytest backend/tests/test_workflow.py::test_operation_target_detail_returns_group_context_messages backend/tests/test_workflow.py::test_operation_target_admission_retry_endpoint_updates_detail_and_audit` passed 2 tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning.
- Independent "紫禁城" first review blocked F010 because retry `reason` accepted whitespace-only input and could write blank audit reasons. Fixed by trimming and rejecting blank reasons in both schema and service, and adding API coverage that whitespace-only reason returns 422. Re-ran F010 verification: operation-target service tests passed 9 selected tests, workflow detail/retry tests passed 2 tests, `python -m compileall app` passed, `npm run build` passed with the existing Vite chunk-size warning, and `git diff --check` passed.
- Independent "紫禁城" second review blocked F010 because the service reused `reason` for an account failure reason, allowing audit `reason=` to be overwritten when a snapshot still returned `can_send=False`. Fixed by separating `operator_reason` from per-account `failure_reason`, and extending the service test with a snapshot-present-but-still-muted account to prove audit retains the operator reason while failed count/reason remain correct. Re-ran F010 verification: operation-target service tests passed 9 selected tests, workflow detail/retry tests passed 2 tests, `python -m compileall app` passed, `npm run build` passed with the existing Vite chunk-size warning, and `git diff --check` passed.
- Independent "紫禁城" review passed F010 after the operator-reason variable fix and serial verification.
- Implementation pass completed for F011 message-send failure rollup before independent review: added message-task failure rollup into `operation_issue` with `operation_issue_sources.source_type=message_task`, target resolution through `operation_targets`, affected account tracking, message-sending return context, target runtime summary refresh, and automatic issue resolution when the same message task is retried and sent successfully. `MessageTaskOut` now exposes `operation_issue_id`, `operation_issue_status`, and `operation_issue_rolled_up`; message sending records show the rollup badge, and operation center message-send issues route back to the message sending page instead of task center.
- Verification before review for F011: the new red/green regression `pytest backend/tests/test_operations_center_runtime.py::test_message_send_failure_rolls_up_operation_issue` passed; targeted runtime/message tests `pytest backend/tests/test_operations_center_runtime.py -k 'message_send or audit_logs_support_operational_filters or operation_center'` passed 5 selected tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F011 because message-send issues are grouped by target and failure type, but auto-resolve closed the whole issue when the latest source message task recovered while other message-task sources were still failed. Fixed by resolving through `operation_issue_sources.source_type=message_task` and checking all related message tasks; if any source task remains `失败`, the issue stays open. Extended the regression with two failed message tasks under the same target/failure type: retrying only the latest keeps the issue open, retrying the remaining failed source resolves it. Re-ran F011 verification: targeted runtime/message tests passed 5 selected tests, `python -m compileall app` passed, `npm run build` passed with the existing Vite chunk-size warning, and `git diff --check` passed.
- Independent "紫禁城" second review blocked F011 because message-sending list status lookup still used the mutable representative `operation_issues.source_task_id`, so older source message tasks in an aggregated issue lost their rollup badge after a newer source replaced the representative id. Fixed both single-task and list-task attachment helpers to join `operation_issue_sources` by `source_type=message_task` and `source_id=<task.id>`. Extended the regression to assert both aggregated tasks show the same open issue in the message-sending list, and after only one task recovers the remaining failed source still shows the open rollup. Re-ran F011 verification: the focused rollup test passed, targeted runtime/message tests passed 5 selected tests, `python -m compileall app` passed, `npm run build` passed with the existing Vite chunk-size warning, and `git diff --check` passed.
- Independent "紫禁城" review passed F011 after the `operation_issue_sources` list-status fix. It also reran the focused regression and reported `1 passed in 0.86s`.
- Implementation pass completed for the first F014/F015 permission-alignment slice before independent review: message sending write permission now uses the PRD name `message_sending.manage`; legacy `message_sending.create` is accepted as an alias but no longer appears in the current permission list or operator template. Backend message-send create/dispatch/retry/cancel write routes require `message_sending.manage`; the admin-user permission modal uses `发送管理`; MessageSendingView hides create/dispatch/retry/cancel controls for view-only users; OperationTargetsView hides message-send deep links for users without message-send manage permission. Added a regression proving a newly created admin with `message_sending.view` + `message_sending.manage` but no legacy `message_sending.create` passes permission middleware for write endpoints, while a view-only user receives 403 with `message_sending.manage`.
- Verification before review for F014/F015 message-sending permission slice: red run failed because `message_sending.manage` was dropped during permission normalization; after the fix, `pytest backend/tests/test_workflow.py::test_message_sending_manage_permission_controls_write_endpoints -q` passed; `pytest backend/tests/test_workflow.py::test_admin_users_permission_lifecycle_and_legacy_subscription_endpoints_removed backend/tests/test_workflow.py::test_message_sending_manage_permission_controls_write_endpoints -q` passed 2 tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed the F014/F015 message-sending permission slice and independently reran the focused workflow regression with `1 passed, 1 warning in 54.83s`.
- Implementation pass completed for the next F014 frontend button-gating slice before independent review: OperationTargetsView now receives explicit permission props and hides target edit/sync/create, detail message sync/comment sync, risk-policy save, account send/listener switches, admission retry, create task, create archive, and message-send deep links according to `targets.manage`, `tasks.manage`, `archives.manage`, and `message_sending.manage`. Verification: `npm run build` passed with the existing Vite chunk-size warning, and `git diff --check` passed.
- Independent "紫禁城" first review blocked the OperationTargetsView gating slice because opening target detail still auto-called `syncTargetMessages(target)`, so a read-only user could trigger `POST /operation-targets/{id}/sync-messages` by clicking the allowed "查看详情" button. Added a focused regression `backend/tests/test_frontend_permission_gating.py::test_operation_target_detail_does_not_auto_sync_for_read_only_users` that failed before the fix, then gated the auto-sync behind `canManageTargets`. Re-ran the regression and it passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed the OperationTargetsView gating slice after the `openDetail` auto-sync permission guard.
- Implementation pass completed for F006 message-send PRD API naming/detail/precheck before independent review: added PRD-named `GET /api/message-send-tasks`, `GET /api/message-send-tasks/{task_id}`, `POST /api/message-send-tasks/{task_id}/precheck`, and PRD-named dispatch/retry/cancel aliases while preserving old `/api/message-tasks` compatibility. The precheck endpoint runs the same risk preflight against the persisted task account/content/schedule and inferred operation target. Frontend refresh and message actions now use `/message-send-tasks` for list/dispatch/retry/cancel. Added a regression proving PRD routes exist, list/detail return the task with rollup fields, precheck returns account and target evidence, and missing PRD-named operation routes return route-level 404 rather than method drift.
- Verification before review for F006: the regression first failed because `GET /api/message-send-tasks` returned 405, then failed again because PRD-named dispatch/retry/cancel routes were missing from `app.routes`; after fixes, `pytest backend/tests/test_workflow.py::test_message_send_task_prd_list_detail_and_precheck_endpoints -q` passed. Focused verification `pytest backend/tests/test_workflow.py::test_message_send_task_prd_list_detail_and_precheck_endpoints backend/tests/test_workflow.py::test_message_sending_manage_permission_controls_write_endpoints -q` passed 2 tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F006 and independently reran the focused PRD route regression with `1 passed, 1 warning in 22.46s`.
- Implementation pass completed for F012 rules-center version governance before independent review: copy/publish/rollback service calls now accept an operator reason and write audit detail with `reason=` plus changed config fields as `diff=...`; publish compares active version to target version, rollback compares active version to rollback source, and copy records `diff=none` when it is a pure draft copy. API routes accept `{reason}` payload for publish/copy/rollback while preserving empty-body compatibility. RulesCenter UI now exposes a copy action in version lists and routes copy/publish/rollback through a structured confirmation modal with target/current version, diff fields, and required reason, instead of silently posting.
- Verification before review for F012: red regression failed because `copy_rule_set_version(..., reason=...)` was unsupported; after fixes, `pytest backend/tests/test_rule_center_refactor.py::test_rule_version_copy_and_rollback_create_traceable_versions -q` passed. Broader rule-version regression `pytest backend/tests/test_rule_center_refactor.py -k 'rule_version or published_rule_version_is_immutable or bound_tasks_show_fixed' -q` passed 4 selected tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F012 because the RulesCenter `编辑配置 / 基于此版编辑` path still saved via `PUT /rule-sets/{id}/config`, immediately published a new version, and wrote only `v新版本` without structured `reason=` or `diff=` evidence. Added a red regression for config-edit publish audit, then fixed `RuleSetVersionCreate.publish_reason`, `update_rule_set_config` active-vs-new diff audit, and the RulesCenter config form so editing an existing ruleset requires a publish reason and sends it in the PUT payload.
- Re-verification for the F012 config-edit fix: `pytest backend/tests/test_rule_center_refactor.py::test_rule_set_config_publish_audit_records_reason_and_diff -q` failed before the fix with detail `v2`, then passed after the fix. Broader F012 regression `pytest backend/tests/test_rule_center_refactor.py -k 'rule_version or published_rule_version_is_immutable or bound_tasks_show_fixed or rule_config_update or rule_set_config_publish' -q` passed 5 selected tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F012 after the config-edit publish reason/diff audit fix.
- Implementation pass completed for F015 permission vocabulary alignment before independent review: added PRD-named `ai.manage`, `prompt_templates.manage`, and `proxies.manage`; removed old `system.secrets_manage` and `accounts.proxy_bind` from the public permission list while mapping those legacy values to the new names; changed AI provider/default-model/content-keyword routes to `ai.manage`, prompt-template routes to `prompt_templates.manage`, proxy/proxy-binding/proxy-alert write routes to `proxies.manage`, and kept message sending on `message_sending.manage`. The admin permission modal now exposes the PRD names, and AI settings action buttons use `ai.manage` / `prompt_templates.manage` instead of system-admin-only checks.
- Verification before review for F015: new red regression `pytest backend/tests/test_permission_vocabulary.py -q` first failed because `ai.manage` was absent; after fixes it passed. Focused verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_workflow.py::test_admin_users_permission_lifecycle_and_legacy_subscription_endpoints_removed backend/tests/test_workflow.py::test_message_sending_manage_permission_controls_write_endpoints -q` passed 3 tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F015 because `RiskControlView` still exposed proxy resource and proxy alert write buttons to users without `proxies.manage`, even though the backend now requires that PRD permission. Fixed by passing `canManageRisk` and `canManageProxies` from `AppShell`, gating global risk-policy edit by `risk.manage`, gating proxy create/edit/check/disable and proxy alert actions by `proxies.manage`, and adding function-level guards plus a static regression for the proxy UI gating.
- Re-verification for the F015 RiskControl UI fix: `pytest backend/tests/test_frontend_permission_gating.py::test_risk_control_proxy_write_actions_require_proxy_manage_permission -q` passed. Focused verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_frontend_permission_gating.py::test_risk_control_proxy_write_actions_require_proxy_manage_permission backend/tests/test_workflow.py::test_admin_users_permission_lifecycle_and_legacy_subscription_endpoints_removed backend/tests/test_workflow.py::test_message_sending_manage_permission_controls_write_endpoints -q` passed 4 tests; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F015 after the RiskControl proxy UI gating fix.
- Implementation pass completed for F004 material PRD API surface before independent review: added material detail, version-history, create-version alias, references, refresh-cache, and material-group list/create/update endpoints; added a `material_groups` table and migration; wired route permissions for material groups, version creation, and cache refresh. The new detail/reference/version endpoints reuse existing material reference summary and material asset/TG reference version history instead of adding parallel state.
- Verification before review for F004: new red regression `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups -q` first failed with `GET /api/materials/{material_id}` returning 405; after fixes it passed. Focused material verification `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups backend/tests/test_workflow.py::test_admin_users_permission_lifecycle_and_legacy_subscription_endpoints_removed backend/tests/test_rule_center_refactor.py -k 'material' -q` passed 18 tests, 21 deselected; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F004 because `refresh_material_cache` accepted `material_type="组合消息"` and moved it to `not_cached`, but the material cache worker only processes 图片/表情包/文件, leaving 组合消息 stuck forever. Fixed by rejecting unsupported refresh-cache material types with 400 instead of creating an unprocessable pending state, and extended the F004 regression to cover this case.
- Re-verification for the F004 refresh-cache fix: `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups -q` passed after first failing on the 组合消息 200 response; focused material verification again passed 18 tests, 21 deselected; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F004 after the unsupported material refresh-cache rejection fix.
- Implementation pass completed for the F013/F014 operation-issue handling slice before independent review: OverviewView now hides operation issue status actions unless AppShell passes `operation_issues.manage`, replaces `window.prompt` with a structured Ant Design reason modal, trims/requires the reason before posting claim/acknowledge/resolve/ignore, and keeps read-only issue/detail navigation available. Added a frontend static regression proving the permission prop wiring, no prompt usage, structured reason state, modal submit path, and gated drawer actions.
- Verification before review for F013/F014 operation-issue handling slice: the new regression `pytest backend/tests/test_frontend_permission_gating.py::test_overview_issue_status_actions_require_permission_and_structured_reason -q` first failed because `canManageOperationIssues` was missing, then passed after the fix. Broader `pytest backend/tests/test_frontend_permission_gating.py -q` passed 3 tests; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F013/F014 because backend permission middleware still required `operation_issues.manage` for GET `/api/operation-issues` and `/api/operation-issues/{id}`, contradicting the read-only viewing acceptance. Fixed GET issue routes to allow `overview.view` or `operation_issues.manage` while keeping all POST status actions on `operation_issues.manage`. Added a direct permission regression plus an HTTP regression proving an `overview.view` user can GET list and reach 404 for a missing detail without 403, while claim/acknowledge/resolve/ignore remain 403 with `operation_issues.manage`.
- Re-verification for the F013/F014 blocker fix: `pytest backend/tests/test_permission_vocabulary.py::test_operation_issue_read_routes_are_view_only_and_status_actions_require_manage -q` failed before the permission fix and passed after it; `pytest backend/tests/test_workflow.py::test_operation_issue_read_only_can_view_but_not_change_status -q` passed; combined verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_frontend_permission_gating.py backend/tests/test_workflow.py::test_operation_issue_read_only_can_view_but_not_change_status -q` passed 6 tests with 1 Alembic warning; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F013/F014 after the read-only GET permission fix.
- Implementation pass completed for F016 sensitive-read direct-call verification before independent review: added explicit backend permission rules for runtime config (`system.view`), account clone plan reads (`accounts.clone`), verification task reads including account/group scoped paths (`accounts.sync`), channel comment reads (`targets.view`), and relay attribution report (`rules.view`). Existing runtime-config workflow tests were updated to call with authenticated headers, matching the new protected route behavior.
- Verification before review for F016: the new direct permission regression `pytest backend/tests/test_permission_vocabulary.py::test_sensitive_read_routes_have_explicit_least_privilege_rules -q` first failed because `/api/config/runtime` had no rule, then passed after adding the rules. Added HTTP regression `pytest backend/tests/test_workflow.py::test_sensitive_read_routes_deny_low_permission_direct_calls -q`, proving an `overview.view`-only user receives 403 for each sensitive read with the expected permission. Combined verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_workflow.py::test_operation_issue_read_only_can_view_but_not_change_status backend/tests/test_workflow.py::test_sensitive_read_routes_deny_low_permission_direct_calls backend/tests/test_workflow.py::test_clean_seed_requires_config_before_account_create backend/tests/test_workflow.py::test_runtime_login_flows_health_and_group_authorize -q` passed 7 tests with 4 Alembic warnings; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F016.
- Implementation pass completed for the remaining F007 operation metrics API surface before independent review: added PRD-named `GET /api/operation-metrics/reports` backed by the existing report builder, and `POST /api/operation-metrics/export` with a required reason, CSV response, and audit record `target_type=operation_metrics,target_id=export`. Added explicit `usage.view` permission coverage for the export route.
- Verification before review for F007 operation metrics reports/export: `pytest backend/tests/test_permission_vocabulary.py::test_operation_metrics_prd_reports_and_export_routes_use_usage_permission -q` first failed because POST `/api/operation-metrics/export` had no permission rule, then passed after the rule was added; `pytest backend/tests/test_workflow.py::test_operation_metrics_prd_reports_and_export_endpoints -q` first failed with 404 for `/api/operation-metrics/reports`, then passed after implementation. Combined verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_workflow.py::test_operation_metrics_prd_reports_and_export_endpoints backend/tests/test_workflow.py::test_group_policy_enforcement_material_usage_and_reports -q` passed 5 tests, skipped 1 legacy flow, with 1 Alembic warning; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" first review blocked F007 because operation metrics export was incorrectly guarded by `usage.view`; PRD distinguishes `usage.view` from `usage.export`, and `usage.export` was missing from the public permission list/admin authorization UI. Fixed by adding `usage.export` to backend permissions, the operator role template, the admin permission modal and template, changing POST `/api/operation-metrics/export` to require `usage.export`, and extending HTTP coverage so `usage.view` can read reports but receives 403 on export while `usage.export` can export and writes audit.
- Re-verification for the F007 `usage.export` fix: targeted permission vocabulary tests passed 2 tests, frontend permission modal regression passed 1 test, operation metrics workflow regression passed 1 test with 1 Alembic warning; combined verification `pytest backend/tests/test_permission_vocabulary.py backend/tests/test_frontend_permission_gating.py backend/tests/test_workflow.py::test_operation_metrics_prd_reports_and_export_endpoints backend/tests/test_workflow.py::test_group_policy_enforcement_material_usage_and_reports -q` passed 9 tests, skipped 1 legacy flow, with 1 Alembic warning; `python -m compileall app` passed; `npm run build` passed with the existing Vite chunk-size warning; `git diff --check` passed.
- Independent "紫禁城" review passed F007 after the `usage.export` permission fix.
- Implementation pass completed for the first F003 material-center UI slice before independent review: MaterialsView now has a per-material detail action that fetches PRD material detail, references, and version history; the detail modal shows preview, usage count, last used time, last cache error, TG cache reference, reference records, asset versions, and TG cache versions. Row and detail modal actions can manually call `/materials/{id}/refresh-cache` with an operator reason, gated by `materials.manage`, while read-only users can still inspect detail/reference/history.
- Verification before review for this F003 slice: the new static regression `pytest backend/tests/test_frontend_permission_gating.py::test_materials_view_exposes_prd_detail_preview_usage_and_cache_actions -q` first failed because MaterialsView had no detail handler, then passed after implementation. `npm run build` first failed because the chosen `Eye` icon was unavailable in the installed lucide version; after removing that icon, `npm run build` passed with the existing Vite chunk-size warning. `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups -q` passed 1 test with 1 Alembic warning. `pytest backend/tests/test_frontend_permission_gating.py -q` passed 5 tests. `git diff --check` passed. A parallel pytest attempt hit a Postgres schema creation race, then the same tests passed when run serially.
- Independent "紫禁城" first review blocked this F003 slice because upload/ZIP materials store local filesystem paths under `MEDIA_ROOT` such as `.../media/material-tmp/...png`, and the preview used `detailMaterial.content` directly, which browsers cannot load. Fixed MaterialsView preview URL normalization so external URLs and existing `/media/...` paths are preserved, while local `/media/material-tmp`, `/media/source-media-tmp`, `/media/previews`, or `/media/tmp` filesystem paths are converted to browser-served `/media/...` URLs.
- Re-verification for the F003 preview URL fix: the static regression was extended to require `materialPreviewSrc`, `material-tmp`, `/media/${relative}`, and `src={materialPreviewSrc(detailMaterial)}`; it failed before the fix and passed after. `npm run build` passed with the existing Vite chunk-size warning; `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups -q` passed 1 test with 1 Alembic warning; `git diff --check` passed.
- Independent "紫禁城" review passed the F003 detail/preview/reference/cache-refresh slice after the preview URL normalization fix.
- Implementation pass completed for the F003 material group management UI slice before independent review: MaterialsView now exposes a "素材组管理" modal that loads `/material-groups`, shows group name/type/material_count/description/status, and supports create, edit, and enable/disable actions through POST/PATCH `/material-groups`, gated by `materials.manage`.
- Verification before review for the F003 material group management UI slice: the static regression `pytest backend/tests/test_frontend_permission_gating.py::test_materials_view_exposes_prd_material_group_management -q` first failed because MaterialsView had no `MaterialGroup` UI, then passed after implementation. `npm run build` passed with the existing Vite chunk-size warning. `pytest backend/tests/test_frontend_permission_gating.py -q` passed 6 tests. `pytest backend/tests/test_workflow.py::test_material_prd_api_surface_detail_versions_references_refresh_and_groups -q` passed 1 test with 1 Alembic warning. `git diff --check` passed.
- Independent "紫禁城" review passed the F003 material group management UI slice.
