# 2026-09-11 剩余账号与搜索修复 Release Gate

## 范围与事实

用户授权继续修复剩余问题。基线 `4edb190c992a13e3d4d4c02a9d41ee6f85856490`，独立工作树，原工作区未提交改动保留。

- 已恢复账号缺少符合实际 A/C App 分布的独立 SV B；33 个账号实际 C App 与全局默认 C App 不同。以该账号实际 C 身份排除 App，并把 C 版本纳入 preview fingerprint。
- 1831、1914 原临时授权已经通过正式 owner 读到真实登录身份；登记曾因读取已失效兼容主授权而失败。持久化成功登录材料，共享备用登记入口，支持精确原 flow 与独立观察者的身份/设备证明、preview/CAS/apply；不能把同 App 观察者计为 A/B 隔离备份。
- 切主实时 self hash=0 时，在原 case 身份/CAS 验证通过后保留原资产已证明的非零 hash，已有身份摘要不得冲突。
- 搜索实际批次 dispatcher 在 claim 前为到期 fact-first 任务补给空闲槽，并在原 Task+wake 锁内重新检查；保留当前并发 2、日目标 1000、未知不重放规则。continuous claim 分支同步接入，不改变 worker 批次/退出模式。

## 源码与自动验证门禁

PRD 与数据流/结构索引先行更新。本地定向验证覆盖新恢复、App 选择、切主、实际搜索批次入口、生命周期与未知保留。最终权威 QA 为冻结提交的原生 `local_release.py prepare` 回执：19 个测试批次，每个后端进程强制 60 秒超时，数据库仅 `tg_yunying_test`；预计 449 项。原测试批次 354 项及新增影响测试完整列于本次证据目录的 `test-selections.json`，不得跳过失败批次。

prepare 同时执行编译/空白检查、前端依赖与构建以及 `linux/amd64` 镜像构建。真实本地 docker load 后核对制品 ID/平台，并运行独立 owner 镜像 smoke（禁止第二 owner、正常退出）；这些检查不证明 Telegram 业务恢复。

## 安装与生产边界

1. 先等现有串行 SV 备份批次结束并读回所有回执，禁止活跃登录/备份途中切换 owner。
2. master/release 与冻结制品一致；核对当前 36 项生产配置、全量 1830 个账号、原已恢复集合和 53 个已停代理容器。无新增失效才进入安装。
3. 本地制品直传。传输若使用内容差异复用，必须本地与远端都重建出与原 prepared manifest 完全一致的完整包，之后仍经原生导入验证及安装，只执行一次安装。失败先读回，不自动重放。
4. 运行 SHA、配置指纹、单 owner 和账号健康读回成功后，按正式精确清理上一成功发布的包与未被引用镜像；保留本轮证据。
5. 对剩余 33 个 B 备份和 1831/1914 的登记/激活，仅使用新版本正式服务、精确 preview/CAS、操作回执与独立 Telegram 身份/消息读取；不重置 2FA、不撤销原授权、不读取 MY 密封 Session 作 SV 业务授权。
6. 搜索自然新 Action 验收必须落到 Task→义务/assignment→Attempt/Gateway→typed click 事实；Bot 无回复与未知动作单列，不能缩小目标或宣布全日目标完成。排名观察当前无活动 Task，不能虚构 E4。

## 不能由本轮代码替代的材料

174 个待恢复账号的最新原 flow 盘点只有 1831/1914 有临时登录材料；另 172 个没有可用的该类材料。若没有合法接码入口/有效授权和所需 2FA，不能伪造恢复、重用他人身份或宣布永久丢失。以最终独立读回报告说明实际恢复数量及仍受材料限制的精确集合。
