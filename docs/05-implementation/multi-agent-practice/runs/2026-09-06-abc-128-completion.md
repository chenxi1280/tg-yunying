# 128 个基础可用账号 ABC 补齐验收

- Intake：用户要求处理基础可用但 ABC 未完整的 128 个账号，并逐项说明原因；后续明确继续执行。原账号范围保持不变。
- 分级与流程：L3，沿用授权灾备与单账号后置初始化合同。生产诊断、产品设计、开发、定向 QA、产品验收和生产读回均保留独立证据。
- 最终状态：119 个账号取得完整 ABC/E4；9 个账号仍阻塞，不能标记整体 `production_fixed`。
- 核验时间：2026-09-06 06:38:02，Asia/Shanghai。
- 生产版本：`28bbfb355a91a1889c3d38979e3f1bc9b21b3ac1`；current 与 19 个应用容器版本一致且健康，本地 health HTTP 200，最新发布 `33995737955` 成功。

## 原始问题与处理结果

| 原始分类 | 数量 | 本次完整 | 仍未完成 |
|---|---:|---:|---:|
| 原申请等待 ABC 审批或执行 | 44 | 42 | 2 |
| B 登录等待可用的当前二步验证密码 | 69 | 69 | 0 |
| B 登录二步验证密码无效 | 4 | 4 | 0 |
| C 迁移二步验证密码无效 | 4 | 4 | 0 |
| 历史登录中断、验证码未取得或远端结果未决 | 7 | 0 | 7 |
| 合计 | 128 | 119 | 9 |

72 个有绑定来源的密码问题账号均已完成。接码页面的“暂无登录验证码，请先发起登录请求”属于未产生新登录材料的状态，不能据此认定链接失效。取得当前密码后，先由 Telegram 验证，再保存加密托管记录；原等待二步验证的会话按原 AuthKey 接续，确认无远端效果的失败则进入正式新 B 流程。外部来源不充当 B/C 的验证码来源，未修改 Telegram 二步验证密码。

账号 298 没有绑定来源。用户补充的 5 个候选只通过内存送入生产校验，没有写入报告或执行日志；第 4 个候选经 Telegram 接受后，正式保存为加密托管密码。新 B/C 批次 `09720f77-4b36-48e8-aa98-f8161834cf49` 随后完成，B operation `e537d74a-f5cf-482f-ba4b-7a8a56083373` 为 `succeeded`，C operation `1dd0aa57-b28a-4c5c-b110-af3fc970b431` 为 `confirmed`，最终 E4 证据有效。

## 未完成账号

| 账号 | 当前原因 |
|---|---|
| 488 | 原 B 验证码未读到，旧临时会话未授权；保留已发码事实，不能伪造无效果。 |
| 571 | 原 B 在发布中断后缺少可恢复临时会话，远端结果未证实。 |
| 589、613、705 | 原 C 发起后中断，缺少可恢复会话或密封包，远端结果未证实。 |
| 1160、1275 | 历史 B 创建超时，缺少可恢复临时会话，维持隔离对账。 |
| 1764 | 本次 B 创建超时，远端结果未知，已正式隔离。 |
| 1794 | 本次 C 连接 Telegram 超时，已成功的 B 保留；C 仍未知，已正式隔离。 |

## 验证与变更边界

- 128/128 原 A 指针未切换。最终使用生产 `completed_abc_evidence_ref` 逐账号核对，119 个均具备 B/C、备份副本、恢复探针及最终 E4 证据；不是仅按批次成功计数。
- 最终 runtime 为 off，claim scope 与 mutation hold 为空，MY ready 且活动客户端为 0，无 approved/running ABC 批次，无尚未隔离的全局 unknown；上述未决账号的 unknown 事实仍保留。
- 本次唯一运行代码修复为 C 未决操作的历史 owner 隔离判断，提交 `b375b9bf072422e11ec9d78a1907e12e97adf8fc`，44 项定向测试通过；发布 `33984749491` 成功。1794 正式 preview/apply/readback 与后续 1795 的真实 ABC/E4 共同验证该隔离缺陷修复，详见同目录 `2026-09-06-post-login-c-retired-owner.md`。
- 并发发布期间在下一项变更前停止，按新鲜版本与状态继续原检查点；没有重复登录未知操作，没有覆盖主工作区其他任务改动。
- 并发发布 `33995222572` 在 PostgreSQL CI 分片失败，build/deploy 均跳过；修正后的 `33995737955` 完整通过 CI、镜像构建与部署。按新生产 SHA 重新读回后，119/9、128/128 原 A、runtime off、无开放批次或未隔离 unknown 均保持不变。
- 全量逐账号明细包含原始原因、最终结果、原 A 是否未切换及 E4 引用，汇总为 119/9/128/128。冻结账号另按用户参考格式导出 331 行，8 个未查到原始来源的链接列留空，未编造链接。
