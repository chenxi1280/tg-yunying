# 当前账号与密码错误全量核查

- Intake：用户要求分别统计严格完成 ABC 备份的账号和当前正常可用账号，并再次尝试所有仍为密码错误的账号。
- 分级：L3 生产只读核查与窄范围登录挑战；没有应用代码或 PRD 行为变更。
- 生产版本：`0b646f05703378f80bcedd03ed079cd0de839a42`，发布 `33997259500` 成功；current、19 个应用容器和 health HTTP 200 已验证。

## 全量账号快照

首次范围为 1636 个未删除账号，逐账号使用生产 `completed_abc_evidence_ref` 检查严格 ABC，并通过当前主会话连接 Telegram，校验授权、账号身份和冻结配置。扫描期间新增 1803–1805，随后又新增 1806–1808；增量探测同时重查发生代际变化的 1804、1805，最终在 1642 个账号上得到无新增、无删除、无代际变化的封闭范围。

| 口径 | 数量 |
|---|---:|
| 未删除账号 | 1642 |
| 严格 ABC 备份/E4 完整 | 1022 |
| 当前基础正常可用 | 1033 |
| 同时满足 ABC 完整和正常可用 | 1019 |
| 正常可用但 ABC 未完整 | 14 |
| 冻结 | 331 |
| 会话异常 | 278 |

会话异常由 188 个 `AuthKeyDuplicatedError`、39 个未授权会话和 51 个无会话组成。严格 ABC 计数要求 B/C、备份副本、恢复探针及最终 E4 均有效；正常可用要求当前 Telegram 会话已授权、身份有效且未冻结，两个数量不能互相替代。

## 密码错误重试

重新从最新 B/C operation、登录 flow 和批量登录 item 三侧取最新记录，并排除已经取得严格 ABC 的历史失败后，当前仍为 `two_fa_invalid` 的账号只有 1149、1151、1152、1154、1155、1156、1157。7 个均已冻结，正常可用账号中的当前密码错误数为 0。

冻结主会话对 `GetPasswordSettings` 返回 `FrozenMethodInvalidError`，不能把该错误判定为候选密码错误。7 个账号均有 `verified_readable` 原始接码来源，旧 B operation 为 `manual_required + confirmed_no_effect + two_fa_invalid`，旧流程没有临时会话或 code hash，因此每个账号获批且只发起一次新登录挑战。新鲜来源均返回验证码和当前 2FA，用户提供的 5 个候选只在内存中逐一比较，7 个账号均无匹配。

最终读回确认：7/7 原 A 未切换、旧 operation 版本和状态未变、托管密码仍为空、ABC 仍未完整；诊断 flow 3755、3756、3757、3759、3760、3761、3763 均停在等待验证码、`authorization_id` 为空，没有提交登录或生成 B/C，会按登录有效期自然过期。候选内容没有写入脚本、JSON 证据或报告。

全局授权灾备 runtime 为 off，MY 活动客户端为 0，没有 approved/running ABC 批次，也没有尚未隔离的 unknown operation。
