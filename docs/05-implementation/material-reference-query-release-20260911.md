# 素材引用查询修复 Release Gate

- level: L2
- release_mode: local_direct
- release_owner: 当前 Codex 任务
- status: local_gate_passed，制品/安装/生产验证待独立证据

## 范围与合同

故障前素材接口会加载租户全量 Action ORM 行。修复为不可变 JSON 引用提取函数、GIN 表达式索引和数据库去重计数；统计规则、素材禁用/恢复和租户权限不变。前端资源请求入口核对后未证实重复加载缺陷，本次不增加缓存、并发限制或超时降级。保留主工作树两项无关未跟踪文件，独立工作树实现。

## 验证

隔离 PostgreSQL 的5项测试覆盖解析与旧规则比对、跨字段去重、跨租户隔离、修改/删除维护、同名异定义迁移拒绝、31万行历史索引构建及查询计划。31万行样本索引命中，查询1.352ms，4个共享块命中，0个Action ORM对象。完整素材列表调用链另1项通过；旧素材功能另有定向回归。每个后端测试进程60秒硬超时。首次索引格式比对与测试JSON绑定失败保留日志，修正后通过。

冻结源码由 prepare 再次执行上述测试、后端编译、前端构建并生成三份 linux/amd64 镜像及schema-v2清单；确切结果见发布证据目录。

## 发布影响与恢复

新增迁移0234_material_reference_index；表达式索引并发创建，扫描历史JSON一次，正常写入自动维护，无业务行更新和异步回填窗口。既有应用可兼容保留新函数和索引的数据库；任何回退仍按实际版本合同，不撤销账号/Telegram事实。若索引构建失败，保留无效索引和迁移回执，查明失败后处理，不自动重放安装。

沿用现有worker切换与36项运行覆盖参数。部署一次，成功后核对SHA、迁移head、索引valid/ready和真实素材查询计划/耗时/应用内存，再按发布合同清理上一轮成功版本的精确包与未引用镜像。

## 验收边界

发布前生产当前3df0c05b，应用主机约7.3GiB内存、4CPU、磁盘余量6.1GiB；这些仅为当时预检。发布后执行只读素材服务验证，至少观察两次主机内存/换页/磁盘读指标。不创建任务、不重放未知调用，不把本次接口修复与历史整机卡死唯一根因混为一谈，也不声明Telegram日目标完成。

外部证据：/private/tmp/tgyunying-material-reference-evidence/，保留全部测试/prepare/deploy/runtime/查询与主机读回。

## 首次生产验证发现与最终查询修订

ce0ed80f 首次安装因索引构建超过backend就绪检查而退出；迁移仍正常推进。只读确认0234完成、backend健康及19旧worker停止后，在原发布锁下仅接续正式worker prepare/start/complete和发布检查，保留初次失败回执及接续脚本哈希。ce0ed80f runtime与36项配置读回通过，不代表接口验收通过。

真实312素材聚合触发15秒查询超时，执行计划为Seq Scan。精确ANALYZE维护已记录，但扩大本地测试证明统计信息不能保证大数组条件走索引。最终改为一条SQL内逐素材ID相关计数，应用仍不读取Action行，也不截断或估算。

修订测试直接在31万行、新建索引、未手工ANALYZE条件下请求312个素材并断言GIN命中。候选SQL通过生产只读隔离进程验证：312个素材聚合9.287ms、完整列表0.296s、6并发0.466～0.577s、峰值内存增量12624KiB、0 Action ORM对象。这是候选验证，最终镜像安装后还需原生服务复验。后续代码修订不增加迁移，沿用已验证0234索引。
